const Admin = ({ children }) => {
  return (
    <>
      <body>
        <div>
          <h1>Time to do admin sh*t let's go?</h1>
        </div>
        {children}
      </body>
    </>
  );
};

export default Admin;
